---
# vars file for nodejs

nodejs_dir:
  RedHat:
    home: apache-maven-3.5.0
    install: /usr/local
    tmp: /tmp

nodejs_packages:
  8.x:
    RedHat:
      pull: https://rpm.nodesource.com/setup_8.x
  7.x:
    RedHat:
      pull: https://rpm.nodesource.com/setup_7.x
  6.x:
    RedHat:
      pull: https://rpm.nodesource.com/setup_6.x
  5.x:
    RedHat:
      pull: https://rpm.nodesource.com/setup_5.x
  4.x:
    RedHat:
      pull: https://rpm.nodesource.com/setup_4.x
  0.12.x:
    RedHat:
      pull: https://rpm.nodesource.com/setup_0.12
  0.10.x:
    RedHat:
      pull: https://rpm.nodesource.com/setup